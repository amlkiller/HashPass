# ==================== HashPass 环境变量配置 ====================
ADMIN_TOKEN=here

# ==================== HMAC 密钥 ====================
# 用于派生邀请码的 HMAC 密钥（hex 编码，64 个十六进制字符 = 256-bit）
# 设置后密钥在重启间持久化，未设置则每次启动随机生成
# 生成方式: python -c "import secrets; print(secrets.token_hex(32))"
# ⚠️ 警告：此密钥严格保密，不要提交到 Git
HASHPASS_HMAC_SECRET=

# ==================== 服务器配置 ====================
# 监听端口（默认 8000）
PORT=8000

# ==================== Difficulty 难度配置 ====================
# 初始难度（前导零位数，范围 1-6）
HASHPASS_DIFFICULTY=1

# 最小难度
HASHPASS_MIN_DIFFICULTY=1

# 最大难度
HASHPASS_MAX_DIFFICULTY=6

# 目标解题时间范围（秒）
# 如果解题时间 < MIN，难度+1（太快）
# 如果解题时间 > MAX，难度-1（太慢）
HASHPASS_TARGET_TIME_MIN=30
HASHPASS_TARGET_TIME_MAX=120

# ==================== Argon2 配置 ====================
# Argon2 时间成本（迭代次数）
HASHPASS_ARGON2_TIME_COST=3

# Argon2 内存成本（KB）
# 默认 65536 KB = 64 MB（内存硬度，防止 GPU 农场）
HASHPASS_ARGON2_MEMORY_COST=65536

HASHPASS_ARGON2_PARALLELISM=1
HASHPASS_WORKER_COUNT=1
# ==================== Turnstile 人机验证 ====================
# Cloudflare Turnstile Site Key（前端使用）
# 获取地址: https://dash.cloudflare.com/?to=/:account/turnstile
TURNSTILE_SITE_KEY=your_site_key_here

# Cloudflare Turnstile Secret Key（后端验证使用）
# ⚠️ 警告：此密钥严格保密，不要提交到 Git
TURNSTILE_SECRET_KEY=your_secret_key_here

# 测试模式（开发环境建议启用）
# true: 使用测试密钥（1x00000000000000000000AA），所有验证自动通过
# false: 使用真实密钥，需要真实的人机验证
TURNSTILE_TEST_MODE=true

# ==================== Webhook 通知 ====================
# Webhook URL（用户获胜时发送 POST 请求）
# 留空则禁用 Webhook 功能
# 格式: https://your-domain.com/api/webhook
WEBHOOK_URL=

# Webhook Bearer Token（可选，用于 Webhook 鉴权）
# 如果配置了 Token，将在请求头中添加: Authorization: Bearer <token>
# ⚠️ 警告：此 Token 严格保密，不要提交到 Git
WEBHOOK_TOKEN=
